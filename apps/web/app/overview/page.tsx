import PlaceholderPanel from "@/components/PlaceholderPanel";

export default function OverviewPage() {
  return (
    <PlaceholderPanel
      title="Overview"
      sprint="Sprint 2"
      description="Once the aggregation pipeline is running, this page will show:
        pipeline run history, total products/stores tracked, last 30-day sales
        volume, active promotion calendar, and a top-10 at-risk SKU table."
    />
  );
}
