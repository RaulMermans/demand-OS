# DemandOS — root-level runtime dependencies for Vercel Python Function (api/index.py).
# Dev/test dependencies (pytest, httpx) are intentionally excluded to keep bundle size small.
# For local development, install from apps/api/pyproject.toml instead:
#   cd apps/api && pip install -e ".[dev]"

fastapi>=0.111.0
python-multipart>=0.0.9
pydantic>=2.7.0
pydantic-settings>=2.2.0
sqlalchemy>=2.0.0
alembic>=1.13.0
python-dotenv>=1.0.0
pandas>=2.2.0
numpy>=1.26.0
scikit-learn>=1.4.0
joblib>=1.3.0
psycopg2-binary>=2.9.0
